---
name: "OLMo 1 7B"
organization: "Allen Institute for AI (AI2)"
release_date: "2024-02-01"
parameters_total: "7B"
context_length: 2048
open_weights: true
paper_url: "https://arxiv.org/abs/2402.00838"
model_card_url: "https://huggingface.co/allenai/OLMo-7B"
disclosure_level: "open"
architecture:
  positional: "RoPE (base 10000)"
  normalization_placement: "Pre-Norm"
  normalization_type: "LayerNorm (non-parametric)"
  qk_norm: false
  activation: "SwiGLU"
  attention: "MHA (32 query heads, 32 KV heads, head dim 128)"
  moe: null
  other:
    - "32 layers, hidden dim 4096, FFN intermediate 11008"
    - "Non-parametric LayerNorm (no learned scale/bias) — distinctive vs Llama's RMSNorm"
    - "2048-token context, no attention bias"
    - "Fully open: weights, training data (Dolma), training code, intermediate checkpoints"
    - "Apache 2.0 license"
source_urls:
  - "https://arxiv.org/abs/2402.00838"
  - "https://huggingface.co/allenai/OLMo-7B"
  - "https://github.com/allenai/OLMo"
notes: "AI2's first fully-open language model. Architecturally close to Llama 1 — MHA + Pre-Norm + RoPE + SwiGLU — but with a non-parametric LayerNorm rather than Llama's RMSNorm. Predecessor to OLMo 2 (which switched to RMSNorm, added QK-Norm, and moved to a reordered Post-Norm placement)."
---

OLMo 1 7B (February 2024) is the Allen Institute for AI's first fully-open language model and
the start of a lineage that emphasizes reproducibility over architectural novelty. The
recipe is close to Llama 1 — Pre-Norm + RoPE + SwiGLU + MHA — chosen not because the team
had nothing to add but because the openness contribution was the priority. The one
deliberate divergence from Llama is the normalization: OLMo 1 uses a **non-parametric
LayerNorm** (no learned scale or bias) rather than Llama's RMSNorm. The team cites training
stability at long horizons as the motivation.

The "fully open" claim refers to four artifacts published together:

1. **Weights** for all checkpoints (every ~250B tokens).
2. **Training data** (Dolma, ~3T tokens, also fully open).
3. **Training code** (the OLMo trainer in PyTorch / Megatron).
4. **Intermediate evaluations** at every checkpoint.

For research on long-horizon training dynamics, hyperparameter ablations, and curriculum
effects, OLMo 1 remains the most complete open artifact in the field — Llama and DeepSeek
release weights and papers but not the training data or intermediate checkpoints.

For the next generation, see [OLMo 2 13B](/llm-arch-kb/models/olmo-2-13b/) — which switched
to RMSNorm, added QK-Norm, and moved the layer-norms into a reordered Post-Norm placement —
and [OLMo 3 32B](/llm-arch-kb/models/olmo-3-32b/), AI2's flagship dense release.
